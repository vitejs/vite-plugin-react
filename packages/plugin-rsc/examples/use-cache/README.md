# RSC `"use cache"`

This example demonstrates one possible framework implementation of React's
`"use cache"` directive. `@vitejs/plugin-rsc` provides the low-level transform
and RSC runtime APIs, but it does not implement cache policy or storage.

```sh
pnpm dev
```
