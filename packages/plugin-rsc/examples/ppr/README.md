# Partial prerendering example

<!-- TODO: Replace with a polished guide to the full demo: differences from examples/starter, build and request flows, persisted artifacts, and the cache/dynamic model. Explain that cache serialization reuses the examples/basic use-cache runtime pattern. Contrast its minimal PPR readiness integration with Next.js's tracked cache/module work and controlled task scheduling, and with vinext's RSC-only fallback-shell implementation, which does not use React DOM postponed/resume. Keep pinned prior-art links beside those comparisons. -->

## Usage

```sh
pnpm dev
pnpm build
pnpm preview
```
